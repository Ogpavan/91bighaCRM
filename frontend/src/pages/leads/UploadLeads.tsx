export default function UploadLeads() {
  return (
    <section className="rounded-sm border border-gray-200 bg-white p-3 shadow-sm">
      <h1 className="text-sm font-semibold text-gray-800">Upload Leads</h1>
      <p className="mt-1 text-xs text-gray-500">Upload CSV or XLS files to bulk import leads.</p>
    </section>
  );
}